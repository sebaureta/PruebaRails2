<%= form_with model: @todo, local:true do |f| %>
  <h1>Ingresar Tarea a Realizar<h1>
    <p><%= f.text_field :details %></p>
<div class="form-group">
  <%= f.label :imagen %>
  <%= f.file_field :image, autofocus: true, class: 'form-control input-sm' %>
</div>
<p><%= f.button :Ingresar, class: 'btn btn-success' %></p>
  <% if @todo.completed == true %>
    <% todo.completed == false %>
    <% end %>
<div class="field">
    <%= f.check_box :completed, :style =>'visibility: hidden' %>
</div>
<% end %>
